import{NextResponse}from"next/server";import{getActorOptions}from"@/lib/equipes-nationales/data";import type{ActorType}from"@/lib/equipes-nationales/types";export const runtime="nodejs";export async function GET(request:Request){try{const type=new URL(request.url).searchParams.get("type") as ActorType;return NextResponse.json({rows:await getActorOptions(type)})}catch(error){console.error(error);return NextResponse.json({error:"Impossible de charger les acteurs."},{status:400})}}
