"use client"
import Link from "next/link";import{useEffect,useState}from"react";import{Badge}from"@/components/ui/badge";import{Button}from"@/components/ui/button";import{Table,TableBody,TableCell,TableHead,TableHeader,TableRow}from"@/components/ui/table";import{formatPeriod}from"@/lib/activites/format";import type{Activity,ActivityParticipant,ActivityReferences}from"@/lib/activites/types"
type Row={activity:Activity;participation:ActivityParticipant}
export function ActorActivities({actorId}:{actorId:string}){const[rows,setRows]=useState<Row[]>([]),[refs,setRefs]=useState<ActivityReferences|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState(false);useEffect(()=>{let active=true;fetch(`/api/activites/acteurs/${encodeURIComponent(actorId)}`,{cache:"no-store"}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error();if(active){setRows(d.rows);setRefs(d.references)}}).catch(()=>active&&setError(true)).finally(()=>active&&setLoading(false));return()=>{active=false}},[actorId]);if(loading)return <p className="p-5 text-sm text-muted-foreground">Chargement des activités…</p>;if(error||!refs)return <p className="rounded-lg border border-destructive/30 p-4 text-sm text-destructive">Impossible de charger les activités.</p>;const name=(items:ActivityReferences["types"],id:string)=>items.find(x=>x.id===id)?.label||"—";return <div className="overflow-x-auto rounded-lg border"><Table><TableHeader><TableRow><TableHead>Activité</TableHead><TableHead>Type</TableHead><TableHead>Rôle</TableHead><TableHead>Période</TableHead><TableHead>Statut participation</TableHead><TableHead/></TableRow></TableHeader><TableBody>{rows.sort((a,b)=>b.activity.date_debut.localeCompare(a.activity.date_debut)).map(({activity,participation})=><TableRow key={participation.id_participation}><TableCell className="font-medium">{activity.nom_activite}</TableCell><TableCell>{name(refs.types,activity.id_type_activite)}</TableCell><TableCell>{participation.role_activite}</TableCell><TableCell>{formatPeriod(activity.date_debut,activity.date_fin)}</TableCell><TableCell><Badge variant="secondary">{participation.statut_participation}</Badge></TableCell><TableCell><Link href={`/dashboard/activites/${activity.id_activite}`}><Button variant="ghost" size="sm">Voir</Button></Link></TableCell></TableRow>)}{!rows.length&&<TableRow><TableCell colSpan={6} className="h-24 text-center text-muted-foreground">Aucune activité enregistrée.</TableCell></TableRow>}</TableBody></Table></div>}
