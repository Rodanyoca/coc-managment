"use client"
import { useState } from "react"
import { Plus } from "lucide-react"
import { toast } from "sonner"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"
import { Sheet, SheetContent, SheetDescription, SheetHeader, SheetTitle } from "@/components/ui/sheet"
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"
import { Textarea } from "@/components/ui/textarea"
import type { CampaignEngagement, CompetitionProgram } from "@/lib/competitions/types"

type Refs = { campaigns: { id:string;label:string;teamId:string;federationId:string;dateStart:string;dateEnd:string;status:string }[]; statuses:{id:string;label:string}[]; federations:{id:string;label:string}[] }
const empty = { id_programme_competition:"", id_campagne:"", id_statut_engagement:"", date_engagement:"", date_debut:"", date_fin:"", id_federation_source:"", date_transmission:"", reference_source:"", observation:"" }
export function CampaignEngagements({ competitionId, programs, initialRows, references, canEdit }: { competitionId:string; programs:CompetitionProgram[]; initialRows:CampaignEngagement[]; references:Refs; canEdit:boolean }) {
 const [rows,setRows]=useState(initialRows),[open,setOpen]=useState(false),[editing,setEditing]=useState(""),[form,setForm]=useState(empty),[saving,setSaving]=useState(false)
 const show=(row?:CampaignEngagement)=>{setEditing(row?.id_engagement_campagne||"");setForm(row?{...empty,...row}:empty);setOpen(true)}
 const campaign=references.campaigns.find((x)=>x.id===form.id_campagne), responsible=campaign?.federationId
 async function save(){setSaving(true);try{const response=await fetch(`/api/competitions/${encodeURIComponent(competitionId)}/engagements`,{method:editing?"PUT":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:editing,row:form})}),result=await response.json();if(!response.ok)throw new Error(result.error);setRows((current)=>editing?current.map((x)=>x.id_engagement_campagne===editing?result.row:x):[...current,result.row]);setOpen(false);toast.success(editing?"Engagement modifié.":"Campagne engagée.")}catch(error){toast.error(error instanceof Error?error.message:String(error))}finally{setSaving(false)}}
 return <><div className="space-y-4"><div className="flex items-center justify-between gap-3"><div><h3 className="font-semibold">Engagements des campagnes</h3><p className="text-sm text-muted-foreground">Une équipe permanente ne participe jamais directement.</p></div>{canEdit&&<Button disabled={!programs.length||!references.campaigns.length||!references.statuses.length} onClick={()=>show()}><Plus className="mr-2 h-4 w-4"/>Engager</Button>}</div>{(!references.statuses.length||!references.campaigns.length)&&<p className="rounded-lg border border-amber-300 bg-amber-50 p-4 text-sm text-amber-900">Campagnes ou statuts d’engagement indisponibles : aucune écriture n’est possible.</p>}<div className="min-w-0 overflow-hidden rounded-lg border"><Table><TableHeader><TableRow><TableHead>Équipe / campagne</TableHead><TableHead>Programme</TableHead><TableHead className="hidden md:table-cell">Fédération</TableHead><TableHead>Statut</TableHead></TableRow></TableHeader><TableBody>{rows.map((row)=><TableRow key={row.id_engagement_campagne}><TableCell className="max-w-64 whitespace-normal"><p className="font-medium">{row.nom_equipe_nationale||row.id_equipe_nationale}</p><p className="text-xs text-muted-foreground">{row.nom_campagne||row.id_campagne}</p></TableCell><TableCell className="max-w-48 whitespace-normal">{row.id_programme_competition||"—"}</TableCell><TableCell className="hidden max-w-48 whitespace-normal md:table-cell">{row.id_federation_responsable||"—"}</TableCell><TableCell><Badge variant="outline">{row.id_statut_engagement||"—"}</Badge></TableCell></TableRow>)}{!rows.length&&<TableRow><TableCell colSpan={4} className="h-24 text-center text-muted-foreground">Aucune campagne engagée.</TableCell></TableRow>}</TableBody></Table></div></div><Sheet open={open} onOpenChange={setOpen}><SheetContent className="w-full overflow-y-auto sm:max-w-lg"><SheetHeader><SheetTitle>{editing?"Modifier l’engagement":"Engager une campagne"}</SheetTitle><SheetDescription>Le programme et la campagne deviennent immuables après création.</SheetDescription></SheetHeader><div className="space-y-4 px-4"><Field label="Programme *"><Select disabled={!!editing} value={form.id_programme_competition} onValueChange={(v)=>setForm({...form,id_programme_competition:v})}><SelectTrigger><SelectValue placeholder="Sélectionner"/></SelectTrigger><SelectContent>{programs.map((x)=><SelectItem key={x.id_programme_competition} value={x.id_programme_competition}>{x.id_programme_competition} · {x.id_epreuve}</SelectItem>)}</SelectContent></Select></Field><Field label="Campagne *"><Select disabled={!!editing} value={form.id_campagne} onValueChange={(v)=>setForm({...form,id_campagne:v})}><SelectTrigger><SelectValue placeholder="Sélectionner"/></SelectTrigger><SelectContent>{references.campaigns.map((x)=><SelectItem key={x.id} value={x.id}>{x.label}</SelectItem>)}</SelectContent></Select></Field>{campaign&&<p className="text-xs text-muted-foreground">Campagne : {campaign.dateStart} — {campaign.dateEnd||"en cours"}</p>}<Field label="Statut *"><Select value={form.id_statut_engagement} onValueChange={(v)=>setForm({...form,id_statut_engagement:v})}><SelectTrigger><SelectValue placeholder="Sélectionner"/></SelectTrigger><SelectContent>{references.statuses.map((x)=><SelectItem key={x.id} value={x.id}>{x.label}</SelectItem>)}</SelectContent></Select></Field>{[["date_engagement","Date d’engagement *"],["date_transmission","Date de transmission *"],["date_debut","Début opérationnel"],["date_fin","Fin opérationnelle"]].map(([key,label])=><Field key={key} label={label}><Input type="date" value={form[key as keyof typeof form]} onChange={(e)=>setForm({...form,[key]:e.target.value})}/></Field>)}<Field label="Fédération source *"><Select value={form.id_federation_source} onValueChange={(v)=>setForm({...form,id_federation_source:v})}><SelectTrigger><SelectValue placeholder="Sélectionner"/></SelectTrigger><SelectContent>{references.federations.map((x)=><SelectItem key={x.id} value={x.id}>{x.label}</SelectItem>)}</SelectContent></Select></Field>{responsible&&<p className="text-xs text-muted-foreground">Fédération responsable : {responsible}</p>}<Field label="Référence source"><Input value={form.reference_source} onChange={(e)=>setForm({...form,reference_source:e.target.value})}/></Field><Field label="Observation"><Textarea value={form.observation} onChange={(e)=>setForm({...form,observation:e.target.value})}/></Field><div className="flex justify-end gap-2"><Button variant="outline" onClick={()=>setOpen(false)}>Annuler</Button><Button disabled={saving} onClick={save}>Enregistrer</Button></div></div></SheetContent></Sheet></>
}
function Field({label,children}:{label:string;children:React.ReactNode}){return <div className="space-y-2"><Label>{label}</Label>{children}</div>}







